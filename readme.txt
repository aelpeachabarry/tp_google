-utilisation du robot google like:
-cliquer sur le fichier racine "indexp.php
-entrer une url à indexer manuellement dans le champ
-démarrer le robot automatiquement pour 50 tours


-ce robot a été fait selon une logique mvc avec du php orienté objet. Il y a trois classe : bibliothèque qui gère des fonctions tels que l'écriture des liens dans un ficheir log et le système de notation,
la classe parsing qui gère toutes les méthodes d'exploration de contenue du dom et database qui gère les appels à la base de données.
